import React from 'react'
import { View, Text } from 'react-native'
import { ThemedText } from '../ThemedText'
import { ThemedView } from '../ThemedView'

export default function WorkoutTracker() {
    return (
        <ThemedView>
            <ThemedText> Workout Tracker</ThemedText>
        
        </ThemedView>
    )
}
